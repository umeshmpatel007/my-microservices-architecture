server.port:
  9001
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/umeshmpatel007/config-server-files.git
          clone-on-start: true
          username: umeshmpatel007
          password: github@pass@007

